# Demo credentials for the PulseBoard sign-in screen.
# NOTE: VITE_* variables are inlined into the client bundle at build time, so
# these are readable by anyone who opens the app. Fine for a workshop demo —
# never put a real secret here.
VITE_AUTH_USERNAME=root
VITE_AUTH_PASSWORD=root
